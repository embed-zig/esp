# hal

Sdkconfig binding for the ESP-IDF `hal` component.

## ESP-IDF component

Internal hardware abstraction layer; no public API documentation (ESP-IDF internal component).

## Module boundary

Provides sdkconfig bindings for HAL assertion levels, SPI master/slave IRAM placement, ECDSA countermeasures, and watchdog ROM implementation selection.

## Dependencies

No runtime dependencies.
