# esp_security

Sdkconfig binding for ESP-IDF security components.

## ESP-IDF component

Aggregates security-related Kconfig keys spanning flash encryption and secure boot subsystems.

## Module boundary

Provides sdkconfig bindings for flash encryption, secure boot (v2/RSA), signed app verification, and ROM download mode security settings. Does not expose runtime APIs.

## Dependencies

No runtime dependencies.
